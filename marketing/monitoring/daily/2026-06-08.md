# Портфолио — ежедневный мониторинг

**Run date (МСК):** 2026-06-08  
**Primary day:** 2026-06-07  
**Last 7 full days:** 2026-06-01 – 2026-06-07

## Статус: RED — API connectivity failure

Все вызовы API Яндекс.Директ и Яндекс.Метрики завершились ошибкой DNS-разрешения:
`[Errno 8] nodename nor servname provided, or not known`

Ошибка воспроизвелась на всех 7 параллельных вызовах:
- `yandex_direct_campaigns` — ❌
- `yandex_direct_report` (primary_day, CAMPAIGN_PERFORMANCE_REPORT) — ❌
- `yandex_direct_report` (7 days, CAMPAIGN_PERFORMANCE_REPORT) — ❌
- `yandex_direct_report` (7 days, SEARCH_QUERY_PERFORMANCE_REPORT) — ❌
- `yandex_direct_keywords` — ❌
- `yandex_metrika_traffic` — ❌
- `yandex_metrika_conversions` — ❌

## Причина

Ошибка `nodename nor servname provided` указывает, что сетевой стек хоста не может разрешить DNS-имя API-эндпоинта Яндекса. Вероятные причины:
- отсутствие интернет-соединения (WiFi/Ethernet отключён);
- DNS-сервер недоступен;
- VPN/proxy недоступен (если используется);
- блокировка на уровне файрвола.

## Факты

- Данные не собраны: ни одной метрики Direct или Метрики за 2026-06-07 и период 2026-06-01–2026-06-07.
- Статус кампании `710165227` не проверен.
- Статус целей Метрики не обновлён.
- Поисковые запросы не проанализированы.

## Рекомендации

1. Проверить сетевое соединение (ping api.direct.yandex.ru, ping api-metrika.yandex.net).
2. Проверить DNS (nslookup api.direct.yandex.ru).
3. Если VPN — проверить его статус.
4. Перезапустить cron-задачу после восстановления сети.
5. При повторении — проверить `~/.hermes/.env` на целостность токенов.

## Ограничения данных

- Все метрики: insufficient data (сеть недоступна).
- CPA: insufficient data.
- Статус целей: NEEDS_GOAL_MAPPING (не было возможности обновить inventory).

## Алерты

- `api_connectivity_failure`: все Yandex API недоступны (DNS).
- `data_collection_failed`: 0 из 7 вызовов выполнены успешно.

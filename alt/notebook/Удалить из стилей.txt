/* УДАЛИТЬ - дублирует .base-card */
.card--competency {
    padding: 40px 30px;
}
.card--competency::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 4px;
    background: var(--gradient-primary);
}

/* УДАЛИТЬ - дублирует .base-icon */
.card__icon {
    composes: base-icon;
}

/* УДАЛИТЬ - дублирует .base-hero */
.hero-section {
    background: linear-gradient(135deg, var(--primary-color) 0%, #2c5aa0 100%);
    color: white;
    text-align: center;
    padding: 80px 0 60px;
}

/* УДАЛИТЬ - дублирует .base-section */
section {
    padding: 80px 0;
}
section:nth-of-type(even) {
    background-color: var(--light-bg-color);
}

2. Избыточные миксины и композиции:
/* УДАЛИТЬ - больше не используются после рефакторинга */
.competencies-section,
.lead-magnet-section, 
.cases-section,
.conversion-section {
    /* Стили теперь применяются через base-section */
}

/* УДАЛИТЬ - неиспользуемые композиции */
.results-hero {
    composes: base-hero;
}
.dashboard-section {
    composes: base-section;
}
.cases-hero {
    composes: base-hero;
}
/* и другие подобные композиции... */

3. Дублирующие стили карточек:
/* УДАЛИТЬ - дублирует base-card */
.case-card {
    background: var(--white-color);
    border-radius: var(--border-radius);
    border: 1px solid var(--border-color);
    overflow: hidden;
    transition: box-shadow 0.3s ease;
    display: flex;
    flex-direction: column;
}

.lead-magnet-card {
    background: var(--white-color);
    color: var(--dark-color);
    border-radius: var(--border-radius-lg);
    box-shadow: var(--card-shadow-hover);
    display: grid;
    grid-template-columns: 1fr 300px;
    overflow: hidden;
}

.conversion-card {
    background: rgba(255, 255, 255, 0.1);
    backdrop-filter: blur(20px);
    border-radius: var(--border-radius-lg);
    padding: 60px;
    text-align: center;
}

4. Избыточные медиа-запросы:
/* УДАЛИТЬ - дублируют адаптивность базовых классов */
@media (max-width: 1024px) {
    .hero-container {
        grid-template-columns: 1fr;
        gap: 40px;
        text-align: center;
    }
    /* и другие медиа-запросы для компонентов, которые теперь используют базовые классы */
}

5. Устаревшие утилитарные классы:
/* УДАЛИТЬ - если не используются */
.btn-primary {
    background: var(--white-color);
    color: var(--primary-color);
}
.btn-secondary {
    background: transparent;
    color: var(--white-color);
    border: 2px solid var(--white-color);
}

Дополнение к заданию: Перед удалением убедиться, что:
1. Все страницы используют обновленные HTML-классы
2. Проверена отзывчивость на всех устройствах
3. Сохранены все анимации и интерактивные элементы
4. Протестированы все состояния (hover, focus, active)
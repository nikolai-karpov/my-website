/* ==========================================================================
   Файл: styles.css
   Описание: Единый файл стилей для сайта-портфолио
   ========================================================================== */

/* --- ОСНОВНЫЕ НАСТРОЙКИ (Переменные) --- */
:root {
/* Цветовая палитра */
    --primary-color: #4A90E2;
    --primary-hover-color: #357ABD;
    --dark-color: #2c3e50;
    --text-color: #34495e;
    --light-bg-color: #f8f9fa;
    --white-color: #ffffff;
    --border-color: #e0e0e0;

    /* Шрифты */
    --font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif, "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol";

    /* Радиус скругления */
    --border-radius: 8px;
}

/* --- ОБЩИЕ СТИЛИ И СБРОС --- */
*, *::before, *::after {
    box-sizing: border-box;
}

body {
    margin: 0;
    font-family: var(--font-family);
    color: var(--text-color);
    background-color: var(--white-color);
    line-height: 1.6;
    font-size: 16px; /* Базовый размер шрифта */
}

.container {
    max-width: 1140px;
    margin-left: auto;
    margin-right: auto;
    padding-left: 20px;
    padding-right: 20px;
}

section {
    padding: 80px 0;
}

section:nth-of-type(even) { /* Чередование фона секций */
    background-color: var(--light-bg-color);
}

img {
    max-width: 100%;
    height: auto;
    display: block;
}

/* --- ТИПОГРАФИЯ --- */
h1, h2, h3 {
    color: var(--dark-color);
    font-weight: 700;
    line-height: 1.3;
    margin: 0;
}

h1 { font-size: 3rem; margin-bottom: 20px; }
h2 { font-size: 2.2rem; text-align: center; margin-bottom: 50px; }
h3 { font-size: 1.5rem; margin-bottom: 15px; }

p {
    margin: 0 0 15px 0;
    font-size: 1.1rem;
}

a {
    color: var(--primary-color);
    text-decoration: none;
    transition: color 0.3s ease;
}

a:hover {
    color: var(--primary-hover-color);
    text-decoration: underline;
}

/* --- КОМПОНЕНТЫ --- */

/* Шапка */
.main-header {
    background-color: var(--white-color);
    padding: 15px 0;
    border-bottom: 1px solid var(--border-color);
    position: sticky;
    top: 0;
    z-index: 1000;
}

.header-container {
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.logo {
    font-size: 1.5rem;
    font-weight: bold;
    color: var(--dark-color);
}
.logo:hover { text-decoration: none; }

nav ul {
    margin: 0;
    padding: 0;
    list-style: none;
    display: flex;
    gap: 25px;
}
nav a { font-weight: 500; }
nav a:hover { text-decoration: none; }


/* Подвал */
.main-footer {
    background-color: var(--dark-color);
    color: rgba(255, 255, 255, 0.7);
    padding: 40px 0;
    text-align: center;
}
.main-footer a { color: var(--white-color); }


/* Кнопка */
.cta-button {
    display: inline-block;
    background-color: var(--primary-color);
    color: var(--white-color);
    padding: 15px 30px;
    border-radius: var(--border-radius);
    font-weight: bold;
    font-size: 1.1rem;
    transition: background-color 0.3s ease, transform 0.2s ease;
}
.cta-button:hover {
    background-color: var(--primary-hover-color);
    color: var(--white-color);
    text-decoration: none;
    transform: translateY(-2px);
}

/* Карточки */
.card {
    background: var(--white-color);
    padding: 30px;
    border-radius: var(--border-radius);
    border: 1px solid var(--border-color);
    text-align: center;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
}
.card:hover {
    transform: translateY(-10px);
    box-shadow: 0 10px 20px rgba(0,0,0,0.05);
}
.card .icon {
    font-size: 3rem;
    margin-bottom: 20px;
    color: var(--primary-color);
}

/* --- СТИЛИ СЕКЦИЙ НА ГЛАВНОЙ СТРАНИЦЕ --- */

/* Герой-блок */
.hero-section { display: flex; align-items: center; gap: 40px; }
.hero-text { flex: 1; }
.hero-image img { width: 250px; height: 250px; border-radius: 50%; object-fit: cover; border: 5px solid var(--light-bg-color); }
.hero-text .subtitle { font-size: 1.3rem; margin-bottom: 30px; }

/* Компетенции */
.competencies-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 30px; }

/* Лид-магнит */
.lead-magnet-section { display: flex; align-items: center; gap: 50px; background-color: var(--dark-color); color: var(--white-color); padding: 60px; border-radius: var(--border-radius); }
.lead-magnet-section h2, .lead-magnet-section h3 { color: var(--white-color); text-align: left; }
.lead-magnet-text { flex: 1; }
.lead-magnet-cover img { width: 200px; border-radius: var(--border-radius); box-shadow: 0 10px 30px rgba(0,0,0,0.2); }

/* Превью кейсов */
.cases-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 30px; }
.case-card { background: var(--white-color); border-radius: var(--border-radius); border: 1px solid var(--border-color); overflow: hidden; transition: box-shadow 0.3s ease; display: flex; flex-direction: column; }
.case-card:hover { box-shadow: 0 10px 30px rgba(0,0,0,0.1); }
.case-card a { color: inherit; text-decoration: none; display: flex; flex-direction: column; height: 100%;}
.case-card a:hover { text-decoration: none; }
.case-card img { width: 100%; height: 250px; object-fit: cover; }
.case-content { padding: 25px; flex-grow: 1; }
.case-result { font-weight: bold; color: var(--primary-color); margin-bottom: 20px; }

/* Финальный CTA */
.final-cta-section { text-align: center; }
.final-cta-section p { max-width: 600px; margin-left: auto; margin-right: auto; margin-bottom: 30px; }

/* --- АДАПТИВНОСТЬ --- */
@media (max-width: 992px) {
    .header-container { flex-direction: column; gap: 15px; }
    nav ul { flex-wrap: wrap; justify-content: center; }
    .competencies-grid { grid-template-columns: 1fr; }
    .cases-grid { grid-template-columns: 1fr; }
    .lead-magnet-section { flex-direction: column; text-align: center; }
    .lead-magnet-section h2 { text-align: center; }
}

@media (max-width: 768px) {
    section { padding: 60px 0; }
    h1 { font-size: 2.5rem; }
    h2 { font-size: 1.8rem; margin-bottom: 30px; }
    .hero-section { flex-direction: column-reverse; text-align: center; }
}